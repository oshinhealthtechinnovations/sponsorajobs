"use client";

/**
 * CandidateProGateModal - DEPRECATED & DISABLED
 * Payment gateway removed across the platform.
 * All candidates receive 100% direct, free access to official ATS application links.
 */
export function CandidateProGateModal() {
  return null;
}
